% \input{\pGlobConfCom/"href-visible.tex"}
\typeout{  ==  ==  ==  ==  ==  \pGlobConfCom/"href-visible.tex"}

% Configures hyperref for visible link borders and colors
% This file assumes that hyperref is loaded in the main configuration (e.g., packages.tex).
% Uncomment the line below to load hyperref directly if used standalone.

% Pass the "colorlinks" option to hyperref with custom colors
\PassOptionsToPackage{colorlinks=true, linkcolor=blue, citecolor=green, urlcolor=red}{hyperref}

% Load the hyperref package (uncomment if standalone)
% \usepackage{hyperref}

% Links are clickable and visibly colored, useful for drafts or debugging.
% Adjust link colors as needed for readability in your workflow or document style.

\endinput  %  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -
